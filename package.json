{
  "name": "buenavistaturf",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "prebuild": "node scripts/gen-thumbs.mjs",
    "thumbs": "node scripts/gen-thumbs.mjs",
    "predev": "node scripts/gen-thumbs.mjs",
    "deploy": "npm run build && npx -y netlify-cli deploy --prod --dir=dist --functions=netlify/functions",
    "deploy:preview": "npm run build && npx -y netlify-cli deploy --dir=dist --functions=netlify/functions"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "@netlify/blobs": "^10.7.13",
    "astro": "^7.2.4",
    "sharp": "^0.35.4"
  }
}
